interface:
  display_name: "Repo Post Skill"
  short_description: "Discover and rank repos from repo_posts"
  default_prompt: "Sync repo_posts data, search with hybrid and regex filters, and return top repositories with links, stars, and latest commit dates."
